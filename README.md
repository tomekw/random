# random

Demonstrate that

```clojure
(. Math (random))
```

returns always the same number.

# How to run

```bash
./script/build.sh
```

and then

```bash
./random-number
```

few times.

You may want to tweak `GRAALVM_HOME` in the script file.
